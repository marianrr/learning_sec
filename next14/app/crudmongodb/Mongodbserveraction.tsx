"use server"
import mongoose from "mongoose"
const mongodbserveraction = async(previousState, formData) => {

const id = formData.get("id1")
const name = formData.get("name1")
const price = formData.get("price1")
const quantity = formData.get("quantity1")
const bname = formData.get("submit")








}